name: Profile Dashboard

on:
  workflow_dispatch:
  schedule:
    - cron: "17 */6 * * *"
  push:
    branches:
      - main
      - chore/profile-analytics-dashboard
    paths:
      - "tools/update-profile-dashboard.py"
      - ".github/workflows/profile-dashboard.yml"

permissions:
  contents: write

concurrency:
  group: profile-dashboard-${{ github.ref }}
  cancel-in-progress: true

jobs:
  update-dashboard:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Check out branch
        uses: actions/checkout@v5
        with:
          ref: ${{ github.ref_name }}
          fetch-depth: 0

      - name: Generate live profile dashboard
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: python tools/update-profile-dashboard.py

      - name: Validate generated dashboard
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path

          readme = Path("README.md")
          if not readme.is_file():
              raise SystemExit("README.md is missing")

          text = readme.read_text(encoding="utf-8")
          required_text = [
              "<!-- RECENT_ACTIVITY_START -->",
              "<!-- RECENT_ACTIVITY_END -->",
              "<!-- REPOSITORY_FRESHNESS_START -->",
              "<!-- REPOSITORY_FRESHNESS_END -->",
              "<!-- PUBLIC_ANALYTICS_START -->",
              "<!-- PUBLIC_ANALYTICS_END -->",
              "Contributions in the Last Year",
              "Public Development Analytics",
          ]

          missing_text = [item for item in required_text if item not in text]

          required_assets = [
              Path("assets/profile/contributions.svg"),
              Path("assets/profile/languages.svg"),
              Path("assets/profile/activity-types.svg"),
              Path("assets/profile/repository-sizes.svg"),
              Path("assets/profile/activity-trend.svg"),
          ]

          missing_assets = [str(path) for path in required_assets if not path.is_file() or path.stat().st_size == 0]

          if missing_text:
              print("Missing README markers/sections:")
              for item in missing_text:
                  print(f"  - {item}")

          if missing_assets:
              print("Missing or empty analytics assets:")
              for item in missing_assets:
                  print(f"  - {item}")

          if missing_text or missing_assets:
              raise SystemExit(1)

          print("Profile analytics validation passed.")
          for path in required_assets:
              print(f"  {path}: {path.stat().st_size} bytes")
          PY

      - name: Commit dashboard refresh
        shell: bash
        run: |
          if git diff --quiet -- README.md assets/profile; then
            echo "Profile dashboard already current."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add README.md assets/profile
          git commit -m "chore: refresh profile analytics"
          git push origin "HEAD:${GITHUB_REF_NAME}"
