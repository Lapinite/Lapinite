name: Profile Dashboard

on:
  workflow_dispatch:
  schedule:
    - cron: "17 */6 * * *"
  push:
    branches:
      - main
      - chore/profile-analytics-dashboard
    paths:
      - "tools/update-profile-dashboard.py"
      - ".github/workflows/profile-dashboard.yml"

permissions:
  contents: write

concurrency:
  group: profile-dashboard-${{ github.ref }}
  cancel-in-progress: true

jobs:
  update-dashboard:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Check out branch
        uses: actions/checkout@v5
        with:
          ref: ${{ github.ref_name }}
          fetch-depth: 0

      - name: Generate live profile dashboard
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: python tools/update-profile-dashboard.py

      - name: Validate generated dashboard
        shell: bash
        run: |
          grep -q '<!-- RECENT_ACTIVITY_START -->' README.md
          grep -q '<!-- RECENT_ACTIVITY_END -->' README.md
          grep -q '<!-- REPOSITORY_FRESHNESS_START -->' README.md
          grep -q '<!-- REPOSITORY_FRESHNESS_END -->' README.md
          grep -q '<!-- PUBLIC_ANALYTICS_START -->' README.md
          grep -q '<!-- PUBLIC_ANALYTICS_END -->' README.md
          grep -q 'Contributions in the Last Year' README.md
          test -s assets/profile/contributions.svg
          test -s assets/profile/languages.svg
          test -s assets/profile/activity-types.svg
          test -s assets/profile/repository-sizes.svg
          test -s assets/profile/activity-trend.svg

      - name: Commit dashboard refresh
        shell: bash
        run: |
          if git diff --quiet -- README.md assets/profile; then
            echo "Profile dashboard already current."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add README.md assets/profile
          git commit -m "chore: refresh profile analytics"
          git push origin "HEAD:${GITHUB_REF_NAME}"
